import { FC } from 'react';

import { Loading } from 'sy-core/components/Elements';
import { Page } from 'sy-core/components/Layout';

// @pause
// @pour.fields(name=User,fields=5,seed=true)

export const Test: FC = () => {
  return <Page>Boilerplate</Page>;
};
